n=100;
val="";
loop = val.rightPad(n).replaceAll(" ", "1,").removeLastOccurence(",").toList();
count=1;
for each rec in loop
{
	info count;
	count=count+1;
}

//rightPad adds spaces to the right n number of times,
//removeLastOccurence removes last comma from the data, 
//and toList we make a list using it
